name: CI/CD - Node.js SSL Server Deployment (Secure + Free GitLeaks)

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  ci-cd:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production

    steps:
      # -----------------------------
      # CI STAGE - Build & Security
      # -----------------------------

      - name: Checkout repository
        uses: actions/checkout@v4

      # 1️⃣ GitLeaks - Free version (Secret Scanner)
      - name: Run GitLeaks via CLI (free version)
        run: |
          echo "🔍 Installing GitLeaks..."
          GITLEAKS_VERSION=$(curl -s https://api.github.com/repos/gitleaks/gitleaks/releases/latest | grep tag_name | cut -d '"' -f 4)
          curl -sSL -o gitleaks.tar.gz https://github.com/gitleaks/gitleaks/releases/download/${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION#v}_linux_x64.tar.gz
          tar -xzf gitleaks.tar.gz
          sudo mv gitleaks /usr/local/bin/
          chmod +x /usr/local/bin/gitleaks

          echo "🚀 Running GitLeaks scan..."
          gitleaks detect --source . --no-banner --redact || (echo "❌ Secrets found! Fix them before deploying." && exit 1)

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Security Audit (vulnerability check)
      - name: Security Audit - Check for vulnerabilities
        run: |
          npm audit --production || echo "⚠️ Vulnerabilities found - please review later"

      # 5️⃣ Build the application
      - name: Build the application
        run: |
          if [ -f package.json ] && grep -q "\"build\"" package.json; then
            npm run build
          else
            echo "ℹ️ No build step found, skipping..."
          fi

      # 6️⃣ Run tests (if any)
      - name: Run tests
        run: |
          if [ -f package.json ] && grep -q "\"test\"" package.json; then
            npm test
          else
            echo "ℹ️ No tests defined, skipping..."
          fi

      # -----------------------------
      # CD STAGE - Deploy to EC2
      # -----------------------------

      # 7️⃣ Copy files to EC2
      - name: Copy files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "."
          target: "/home/ubuntu/nodejs-ssl-server"
          rm: true  # removes old files before copying

      # 8️⃣ SSH into EC2 and deploy app
      - name: Deploy and Restart on EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "🔐 Connecting to EC2..."
            cd /home/ubuntu/nodejs-ssl-server

            echo "📦 Installing dependencies..."
            npm install --production

            echo "🚀 Starting or restarting app with PM2..."
            if ! command -v pm2 &> /dev/null
            then
              sudo npm install -g pm2
            fi

            pm2 stop nodejs-ssl-server || true
            pm2 start server.js --name nodejs-ssl-server
            pm2 save

            echo "✅ Deployment complete!"
